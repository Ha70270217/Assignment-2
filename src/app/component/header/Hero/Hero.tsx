import styles from './hero.module.css';

const Hero = () => {
  return (
    <section className={styles.hero}>
        
      <h2>Welcome to the Hero Section</h2>
      
    </section>
  );
};

export default Hero;